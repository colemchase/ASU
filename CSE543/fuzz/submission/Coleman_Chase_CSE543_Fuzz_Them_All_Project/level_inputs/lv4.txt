For this level, I reused the same mutation based fuzzer against the level 4 target binary. The fuzzer generated many randomized inputs, ran /challenge/prog with each one, and watched for crash return codes. It found a 300 byte input that caused a segmentation fault, saved it as crash_0, and I verified it with /challenge/challenge < crash_0, which reproduced the crash and returned the flag.

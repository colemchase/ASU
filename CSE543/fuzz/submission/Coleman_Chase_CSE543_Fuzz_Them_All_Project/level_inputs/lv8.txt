For this level, I reused the mutation based fuzzer against the level 8 binary. The fuzzer found a 186 byte input that caused a segmentation fault, saved it as crash_0, and I verified it with /challenge/challenge < crash_0, which reproduced the crash and returned the flag.
